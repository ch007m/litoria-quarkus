generator:
  source: "./source" # directory path or name of the file to be processed
  destination: "generated"
  image: "${source}/image" # path to the images directory used to resolve and embed images during generation

  metadata:
    author: "First & Last Name"
    email: "user@domain"
    # date is auto-filled with today's date if not set

asciidoctor:
  attributes:
    showtitle: ''
    stylesheet: 'foundation.css'
    stylesdir: 'css'
    nofooter: 'yes'
    icons: 'font'
  options:
    doctype: 'article'
    safe: 'unsafe'

pdf:
  format: A4
  orientation: portrait
  zoomFactor: "0.55"
  border:
    top: 20px
    right: 15px
    bottom: 20px
    left: 15px

# SMTP configuration (used by the 'send' command)
# smtp:
#   host: "smtp.gmail.com"
#   port: 587
#   secure: false
#   requireTLS: true
#   tls:
#     rejectUnauthorized: false
#   user: "your-email@gmail.com"
#   # Auth option 1: App Password (enable 2FA, generate at https://myaccount.google.com/apppasswords)
#   pass: "your-app-password"
#   # Auth option 2: OAuth2 (remove pass above and use this section)
#   # oauth2:
#   #   clientId: "your-client-id"
#   #   clientSecret: "your-client-secret"
#   #   refreshToken: "your-refresh-token"

# Mail configuration (used by the 'send' command)
# mail:
#   from: "user@domain"
#   to: "user@domain"
#   subject: "{author}'s weekly report : {date}"
#   signature: "Cheers{break}----{break}{author}{break}YOUR_TITLE"
